# CLAUDE.md

This project uses **AGENTS.md** as the canonical agent orientation document. Read that file first.

Key references:
- `AGENTS.md` — repo conventions, subagent fleet, invariants
- `docs/architecture.md` — pipeline, scanner contract, cross-link pass
- `docs/schema.md` — JSON schema reference (companion to `schema/loom-index.schema.json`)
- `docs/scanners/` — per-scanner behavior docs: what each detects, edge cases, known limitations
- `docs/contributing.md` — toolchain, Docker workflow, how to add a scanner
